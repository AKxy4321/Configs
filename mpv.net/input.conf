_           script-message-to mpvnet open-files  # File > Open Files...
_           script-message-to mpvnet open-clipboard  # File > Open URL or file from clipboard
_           script-message-to mpvnet open-clipboard append  # File > Add files/URLs to playlist from clipboard
_           stop                          # Playback > Stop
_           playlist-prev                 # Navigate > Previous File
_           playlist-next                 # Navigate > Next File
_           script-message-to mpvnet playlist-first  # Navigate > First File
_           script-message-to mpvnet playlist-last  # Navigate > Last File
_           add chapter 1                 # Navigate > Next Chapter
_           add chapter -1                # Navigate > Previous Chapter
_           frame-step                    # Navigate > Jump To Next Frame
_           frame-back-step               # Navigate > Jump To Previous Frame
_           seek 300                      # Navigate > Jump 5 min forward
_           seek -300                     # Navigate > Jump 5 min backward
_           add video-zoom -0.1           # Pan & Scan > Decrease Size
_           add video-zoom 0.1            # Pan & Scan > Increase Size
_           add video-pan-x -0.01         # Pan & Scan > Move Left
_           add video-pan-x 0.01          # Pan & Scan > Move Right
_           add video-pan-y -0.01         # Pan & Scan > Move Up
_           add video-pan-y 0.01          # Pan & Scan > Move Down
_           add panscan -0.1              # Pan & Scan > Decrease Height
_           add panscan 0.1               # Pan & Scan > Increase Height
_           set video-zoom 0; set video-pan-x 0; set video-pan-y 0  # Pan & Scan > Reset
_           add contrast -1               # Video > Decrease Contrast
_           add contrast 1                # Video > Increase Contrast
_           add brightness -1             # Video > Decrease Brightness
_           add brightness 1              # Video > Increase Brightness
_           add gamma -1                  # Video > Decrease Gamma
_           add gamma 1                   # Video > Increase Gamma
_           add saturation -1             # Video > Decrease Saturation
_           add saturation 1              # Video > Increase Saturation
_           async screenshot              # Video > Take Screenshot
_           async screenshot video        # Video > Take Screenshot without subtitles
_           cycle deinterlace             # Video > Toggle Deinterlace
_           cycle-values video-aspect-override 16:9 4:3 2.35:1 0 -1  # Video > Change Aspect Ratio
_           cycle-values video-rotate 90 180 270 0  # Video > Rotate Video
_           script-message-to mpvnet cycle-audio  # Audio > Next Track
_           add audio-delay 0.1           # Audio > Delay +0.1
_           add audio-delay -0.1          # Audio > Delay -0.1
_           script-message-to mpvnet cycle-subtitles  # Subtitle > Next Track
_           cycle sub-visibility          # Subtitle > Toggle Visibility
_           add sub-delay -0.1            # Subtitle > Delay -0.1
_           add sub-delay 0.1             # Subtitle > Delay +0.1
_           add sub-pos -1                # Subtitle > Move Up
_           add sub-pos 1                 # Subtitle > Move Down
_           add sub-scale -0.1            # Subtitle > Decrease Font Size
_           add sub-scale 0.1             # Subtitle > Increase Font Size
_           cycle-values sub-ass-override force no  # Subtitle > More > Toggle overriding SSA/ASS styles with normal styles
w           add volume 5                  # Volume > Up
s           add volume -5                 # Volume > Down
_           cycle mute                    # Volume > Mute
_           set speed 1                   # Speed > Reset
_           script-binding select/select-playlist  # View > Playlist
_           script-binding stats/display-stats-toggle  # View > Toggle Statistics
_           script-binding osc/visibility  # View > Toggle OSC Visibility
_           script-message-to mpvnet show-media-info osd  # View > Media Info On-Screen
_           script-message-to mpvnet show-media-info msgbox  # View > Media Info Message Box
_           show-progress                 # View > Progress
a           playlist-prev                 # Previous File
d           playlist-next                 # Next File
a           playlist-prev                 # Previous File
d           playlist-next                 # Next File
a           playlist-prev                 # Previous File
d           playlist-next                 # Next File
_           playlist-next
s           playlist-prev

Up          add volume 5                  # Navigate > Jump 30 sec forward
Down        add volume -5                 # Navigate > Jump 30 sec backward
#Volume > Up
#Volume > Down
#Volume > Mute
#Volume Down
#Volume Down
